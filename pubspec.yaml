name: app_movil_cam_maps
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.5.0

dependencies:
  dio: ^5.4.0
  flutter:
    sdk: flutter
  flutter_map: ^7.0.2
  latlong2: ^0.9.1
  cupertino_icons: ^1.0.6
  map: ^2.0.2
  file_picker: ^8.1.2
  http: ^1.6.0
  shared_preferences: ^2.5.4
  fl_chart: ^1.1.1
  url_launcher: ^6.3.2
  video_player: ^2.11.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  assets:
    - images/